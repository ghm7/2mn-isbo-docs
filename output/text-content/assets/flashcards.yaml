- front: What does the trailing slash in `create_assets/` indicate?
  back: The trailing slash indicates that `create_assets` is a directory or folder.
  tags:
  - filesystem
  - notation
- front: What is the directory name in the path `create_assets/`?
  back: The directory name is `create_assets`.
  tags:
  - filesystem
  - path
- front: Why might a path be written as `create_assets/` instead of `create_assets`?
  back: Writing it with a trailing slash makes clear that it refers to a directory
    rather than a file.
  tags:
  - filesystem
  - clarity
- front: How would you describe `create_assets/` in plain language?
  back: '`create_assets/` is a folder named `create_assets`.'
  tags:
  - filesystem
  - description
- front: 'Compare `create_assets/` and `create_assets`: what extra meaning does the
    slash add?'
  back: The slash adds the meaning that `create_assets` is being treated specifically
    as a directory path.
  tags:
  - filesystem
  - comparison
